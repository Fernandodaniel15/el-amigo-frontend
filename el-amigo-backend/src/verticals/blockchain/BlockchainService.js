class BlockchainService {
  async notarize(data) {
    // Conectar a blockchain (ej: Ethereum, Polygon), guardar hash de data
  }
  async verifyNotarization(hash) {
    // Consultar blockchain y devolver resultado
  }
}
module.exports = BlockchainService;
